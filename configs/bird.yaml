data:
  class_path: src.datamodules.Text2Schema
  init_args:
    data_files:
      train:
        - data/train_bird.json
      test:
        - data/dev_bird.json
    batch_size: 32
    num_workers: 8
    pin_memory: true
